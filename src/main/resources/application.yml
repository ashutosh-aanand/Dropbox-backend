spring:
  application:
    name: dropbox-backend
  datasource:
    url: 'jdbc:mysql://localhost:3306/dropbox_db'
    username: root
    password: pass@1234$
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
